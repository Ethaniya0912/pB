# Step 1 단일 에디터 근사 측정 증빙 — 2026-06-13

> 사이클 `2026-06-13_netcode2` · 측정자: 하네스(unity-cli) 단일 에디터 · Unity 6000.3.1f1
> 세션폴더: `…/LocalLow/DefaultCompany/pA for MP with URP/NetDiagnostics/20260613_142640_pid17540`

## V1 정합 검증
- `editor refresh --compile` → 계측 관련 CS 에러 **0**. Step 1 코드 + 직전 Step 0(PROF/채널화) 정합 공존.
- 플레이 진입 → `[NetDiagnostics]` GO 컴포넌트 **7종** 부착(NetEventLogger·StateChecksumV0·BoundaryEchoHarness·SoakHarness·RnsmHud·RuntimeNetStatsMonitor·NetSimController).

## M1 — Transport 보고 RTT
- `StartHost()`=True, isHost=True → `GetCurrentRtt(0)` = **0ms**.
- 판정: loopback에서 0 정상(P0-4 구현됨, 로컬 클라 RTT는 0). Step 0 Before(M1=0)와 일치. **원격 클라 RTT 추종은 2인 측정 대기.**

## M3 — 끊김 이벤트 정합 (P0-2)
StartHost → `Shutdown()` 후 `events.csv` 발췌:
```
9.152,HOST,NGO,"OnServerStarted"
9.152,HOST,NGO,"OnClientStarted"
9.153,HOST,NGO,"OnClientConnectedCallback clientId=0 connectedCount=1"
9.168,HOST,CHECKSUM,"server handler registered"
25.546,HOST,TRANSPORT-RAW,"Transport.Shutdown — 소켓/연결만 정리 (P1-1 수정)"
25.552,OFFLINE,NGO,"OnClientDisconnectCallback clientId=0 reason='…[TransportShutdown] NetworkConnectionManager was shutdown.'"
25.554,OFFLINE,NGO,"OnClientStopped wasHost=True"
25.554,OFFLINE,NGO,"OnServerStopped wasHost=True"
```
- 판정: 끊김 정리 체인이 `OnClientDisconnectCallback`까지 정상 도달, **`Connect` 오발화 0**. P1-1 마커("소켓/연결만 정리") 동반.
- **한계**: 이는 host-local 종료 경로. 원본 P0-2 버그(원격 클라 이탈 시 `ServerCallbacks.OnDisconnected`가 `Connect` 발화)는 코드상 `NetworkEvent.Disconnect`로 교정 확인(L141)이나, **런타임 입증은 원격 피어 필요(2인)**.

## M8 — 재호스팅 (P1-1)
StartHost↔Shutdown ×3:
```
[rehost 1] steamValid=True startHost=True isHost=True err=
[rehost 2] steamValid=True startHost=True isHost=True err=
[rehost 3] steamValid=True startHost=True isHost=True err=
→ StartServer/Steam 에러 0
```
- 판정: 3회 재호스팅 전부 `SteamClient.IsValid` 유지·StartHost 성공·에러 0. **P1-1 효과 입증**(Transport.Shutdown이 Steam API를 끄지 않아 재호스팅 안정). **정량 10/10·유령 클라 0은 2인 측정 대기.**

## MPPM 2피어 1회 실증 (G2 결정)
- Multiplayer Play Mode 2.0.1 설치 확인. 그러나:
  - 런타임/exec로 virtual player를 **활성화·구동하는 공개 API 없음**(GUI `Window > Multiplayer Play Mode` 수동 활성화 전용, unity-cli 비연결).
  - 설령 GUI로 띄워도 virtual player는 동일 머신·**동일 Steam 계정** → [[Facepunch-Steamworks]] P2P 릴레이 **self-connect 불가**.
- **실증 결론**: 단일 머신 2피어 측정 불가 확정. M2 경계값·정량 M8·SCN-07 soak는 **2인 실기기 필수**(계획 §7과 일치).

## 종합
| 지표 | 단일 에디터 결과 | 2인 측정 대기 |
|---|---|---|
| M1 RTT | ✅ 0ms (loopback 정상) | 원격 RTT 추종 |
| M3 끊김 | ✅ Disconnect 체인 정상·Connect 오발화 0 | 원격 클라 이탈 경로·유령 0 |
| M8 재호스팅 | ✅ Steam 생존·에러 0 (×3) | 정량 10/10·유령 |
| M2 경계값 | ❌ host 루프백 무의미 | F9 원격 클라 512~64KB |
| SCN-07 soak | ❌ 시간·2인 | 30분 무중단(데모 게이트 1차) |
